# Ответ Claude владельцу

СТАТУС: ЖДЁТ

ЗАДАЧА: Исправить причину того, что после установки 30 дней срока в CRM для UA-0009, UA-0010 и UA-0011 карточки на сайте обновились не полностью (у UA-0009 в описании осталась старая дата, UA-0010 вообще не пересобралась), и сделать так, чтобы это не повторялось ни для текущих, ни для будущих карточек.

ЧТО СДЕЛАНО:
- Подготовлено полноценное техническое решение (release candidate): единая "транзакция" сохранения срока (дни + дата + время обновления сохраняются вместе), обязательная проверка того, что данные реально записались (read-back), и запуск пересборки/публикации карточки, обоих каталогов и обеих версий (/video и /site) — только после этого CRM имеет право показать "успешно". Если что-то не удалось на любом шаге — успех не показывается, изменения откатываются, задача ставится на повтор.
- Из свободного текстового описания убирается устаревшая конкретная дата доставки, чтобы она не спорила с датой в основном блоке ETA.
- Логика написана универсально — без привязки к конкретным номерам машин, поэтому будет работать и для будущих карточек.
- Написан большой набор автоматических тестов (в том числе имитирующих именно те поломки, что были описаны: обрыв на середине записи, "зависание" очереди, сбой публикации) — все они проверяются в изолированной тестовой среде, без прикосновения к реальной CRM.
- Подготовлены (но НЕ запущены) два рабочих процесса: (1) только для чтения — снять точные данные с реальной CRM и сайта; (2) для боевого включения — сработает только по отдельной точной команде-паролю от владельца и само по себе ничего не меняет на сайте.

ЧТО НУЖНО ОТ ВЛАДЕЛЬЦА: Ничего срочного. У Claude сейчас нет доступа к реальной CRM и реальному сайту, поэтому точную "боевую" причину сбоя по UA-0010 (запись в базу, проверка, очередь, публикация или пересборка) ещё предстоит подтвердить отдельным запуском проверки с реальным доступом (через ChatGPT/Codex или сервер). Пока этого не произошло — считать, что карточки ИСПРАВЛЕНЫ, преждевременно.

СОЗДАННЫЕ ФАЙЛЫ: cloud/task_076_eta_sync/ (README.md, eta_engine.py, eta_transaction.py, sqlite_adapter_reference.py, patch_notes.md, fixtures/, tests/, run_tests.py, workflows/gate_a_readonly.yml, workflows/gate_b_manual_production.yml, evidence/gate_a_evidence.md, evidence/canary_report.md, task_report.md), cloud/latest_status.md, cloud/owner_reply.md

БЕЗОПАСНОСТЬ: Production, CRM и PythonAnywhere НЕ затронуты. Никаких боевых записей не выполнялось.

Служебные метки памяти (не менять): CONTEXT_BUNDLE_SHA256=2187f2edb78a05d8fdfc704059bbacddfc549c2d9e162f5c0ffc2a2e198ce79c; MEMORY_VERSION_READ=4
MEMORY_VERSION_READ: 4
CONTEXT_BUNDLE_SHA256: 2187f2edb78a05d8fdfc704059bbacddfc549c2d9e162f5c0ffc2a2e198ce79c
