Артём, техзадание TASK116 подготовлено и разделено на шесть безопасных этапов:

- A — точный аудит и BEFORE-снимок;
- B — постоянная защита дополнительной спецификации;
- C — автоматическое заполнение основной спецификации после VIN;
- D — удаление четырёх кнопок-дублей без потери истории;
- E — устранение причины ошибки публикации `UA-0017` и защита аналогичных
  карточек;
- F — изолированная проверка, rollback, Gate B и отдельный выпуск.

По скриншоту причина конкретного отказа подтверждается: сборщик остановил публикацию, потому
что для `UA-0017` увидел `0` готовых строк дополнительной спецификации. Он
выполнил полный откат. Текущее Production-состояние без удалённой проверки
честно остаётся `UNKNOWN_REMOTE_NOT_OBSERVED`.

В рамках подготовки этого пакета Production не менялся. Локальные проверки
candidate прошли 14/14, но это не Gate B и не разрешение на публикацию.
Candidate фиксируется в отдельной ветке
`codex/ua-art-crm-spec-publish-recovery-001`; branch/head подтверждаются через
GitHub evidence. Серверный каталог `/home/Carix` для этого не используется.

Exact VIN `UA-0017` пока не подтверждён. VIN публичной `UA-0002` к ней не
привязываем; перед Gate A точный VIN необходимо получить из CRM без догадок.

Текущий честный статус: `UA-0017` заблокирована до read-only Remote Gate A,
проверенного Gate B и вашей отдельной команды. После успешного Gate B для
публикации только этой карточки потребуется точная команда:

`ПУБЛИКОВАТЬ UA-0017`

До этой команды сайт, CRM и Production остаются без изменений. 📋🔒
