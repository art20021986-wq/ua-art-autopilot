# UA ART — Morning Card Check (one screen, no technical knowledge needed)

1. Upload/pull the one file: `uaart_card_factory_gate.py` from this repo's
   `cloud/` folder into your PythonAnywhere account.

2. Put it exactly here:
   `/home/Carix/uaart_card_factory_gate.py`

3. Run exactly this command in a PythonAnywhere Bash console:
   `python3.10 /home/Carix/uaart_card_factory_gate.py`
   (to check different cards later, add their numbers, e.g.
   `python3.10 /home/Carix/uaart_card_factory_gate.py UA-0011 UA-0012`)

4. Open the report here:
   `/home/Carix/video/uaart_card_factory_report.txt`

5. Send that report back. Do not publish anything yourself — publication
   always needs a separate explicit approval after the visual check.
