# TASK 049 — Единый центр «Этапы и доставка» — отчёт Gate A

CONTEXT_BUNDLE_SHA256: 2187f2edb78a05d8fdfc704059bbacddfc549c2d9e162f5c0ffc2a2e198ce79c
MEMORY_VERSION_READ: 4

## Что запрошено

- UA-0006: контейнер `ONEYSELGF1046602` — по discovery уже ALREADY_CORRECT, записи в crm.db не требуется.
- Убрать отдельные входы «Сменить этап», «Срок доставки», «Дней до прибытия», «Номер контейнера» из внешних меню.
- Оставить единственный вход `🚢 Этапы и доставка` в карточке и в редакторе карточки.
- Внутри хаба — смена этапа, номера контейнера, дней до прибытия для уже выбранной карточки.
- Удалить дублирующую кнопку «Изменить срок доставки» после смены этапа.
- Переименовать подпись поля ETA на «количество дней до прибытия».
- Выполнить только Gate A (кандидат-копия), без замены продакшн-файла.

## Что сделано в этом раунде

1. UA-0006: подтверждено ранее discovery — контейнер уже равен `ONEYSELGF1046602`, дополнительных действий с базой не требуется. БД не изменялась.
2. Подготовлен инструмент Gate A: `cloud/tools/task_049_gate_a.py`.
   - Инструмент читает `/home/Carix/cars_ui.py` только на чтение.
   - Пишет исключительно в `/home/Carix/autopilot_inbox/task_049/`.
   - Проверяет SHA-256 источника против связанного значения
     `06e7da916ea36c4ffafef01c85f59a0574f06d3cdda24aa1f8c242175de726b7`.
   - Проверяет обязательные якоря (кнопка «Сменить этап», кнопка «Срок доставки»,
     ключи `eta_days` и `sea_container` в EDITABLE, дублирующая кнопка
     «Изменить срок доставки») — ровно один раз каждый.
   - Строит изолированную кандидат-копию с требуемыми преобразованиями:
     объединение входов в единый `🚢 Этапы и доставка`, удаление `eta_days`
     и `sea_container` из общей сетки редактора, удаление дублирующей кнопки,
     переименование подписи ETA.
   - Компилирует кандидат (`py_compile`) и проверяет пост-условия.
   - Пишет ограниченную JSON-квитанцию в
     `/home/Carix/autopilot_inbox/task_049/gate_a_receipt.json`.
   - Ни при каком исходе не изменяет `/home/Carix/cars_ui.py`, не пишет `crm.db`,
     не перезапускает процессы, не публикует UA-0009, не выполняет Gate B.

## Важное ограничение этого раунда

Среда исполнения Claude/Cloud в этом раунде **не имеет доступа** к файловой
системе PythonAnywhere (`/home/Carix/cars_ui.py` недоступен из этой песочницы).
Поэтому фактический запуск Gate A против реального файла в этом раунде не
выполнялся — инструмент подготовлен, детерминирован и безопасен по
конструкции (SHA-gate, якорные проверки, компиляция, запись только в inbox),
но **не был исполнён** здесь. Это соответствует прежнему прецеденту
(TASK 021): пакет поставляется `READY_FOR_GATE_A_EXECUTION`, реальное
исполнение выполняет процесс/оператор с доступом к продакшн-файловой системе
(например, независимый контроллер на стороне PythonAnywhere), после чего
quitance аудируется ChatGPT/Codex.

## Требуется от владельца / оператора

1. Запустить `cloud/tools/task_049_gate_a.py` в среде, где реально существует
   `/home/Carix/cars_ui.py` (например, консоль PythonAnywhere).
2. Передать содержимое `/home/Carix/autopilot_inbox/task_049/gate_a_receipt.json`
   обратно в канал ChatGPT/Codex для аудита.
3. Только после аудита и явного ответа владельца `APPROVE` может быть
   рассмотрен отдельный Gate B (замена продакшн-файла) — в этом раунде
   Gate B не реализован и не запускался.

## Гарантии безопасности этого раунда

- PRODUCTION_TOUCHED: NO
- CRM_DB_TOUCHED: NO
- RESTART_OR_RELOAD: NO
- UA-0009 PUBLISHED: NO (ua0009_safe_to_publish remains NO per shared memory)
- GATE_B_EXECUTED: NO
