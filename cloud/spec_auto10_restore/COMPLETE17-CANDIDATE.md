# Кандидат final17: спецификация и согласование внешних записей

Подготовлен отдельный состав **UA-ART-SPEC-AUTO10-COMPLETE-17-V1**. Манифест: `d800e55813b6edc24322f7021e924dd0052915f07414b42bd02f0a7c234522f5`, [точный список всех 17 путей](evidence/complete17-candidate-manifest.json). Это сборка и компиляция, не установка и не подтверждение совместного исполнения final17.

| Состав | Действие |
| --- | --- |
| Пятнадцать файлов final15 | Сохранены точные байты манифеста `c4f75a818156e29426c9d601552ab0de56663e965233f2a56c01f5aaa111de07`; исходный каталог, файлы и доказательства не изменены |
| `/home/Carix/analitika_wsgi.py` | Чистое преобразование точного захваченного исходника; проверка общего publication lock и устойчивого признака окна обслуживания перед необязательной записью HTML |
| `/home/Carix/autopilot_inbox/cloud/task_083_catalog_dedup/installer.py` | Чистое преобразование трёх входов фактического installer; общий publication lock и устойчивый признак окна обслуживания, сохранение прежнего порядка внутренних locks |

Вложенный путь installer сохранён полностью: корневого `installer.py` в пакете нет. `stranica.py`, `catalog_design_guard.py` и golden catalog HTML остаются тремя фиксированными зависимостями исполнения; они не добавляются к устанавливаемым 17 файлам.

`complete17_candidate.py` требует точные SHA исходников, чистых patcher и их результатов. Он не импортирует приложения. Новый эксклюзивный каталог создаётся без перезаписи; файлы и вложенные каталоги синхронизируются на диск, манифест записывается последним. При ошибке готовности удаляется только собственный манифест с тем же inode; незавершённый каталог без манифеста не допускается к установке. Повторное чтение сверяет весь набор, SHA и исходные входы. Поля `production_changed=false`, `combined17_execution=NOT_RUN`, `overall_gate_b=NOT_EVALUATED` проверяются строго и не допускают подмены на PASS.

**Восемь проверок assembler прошли:** сохранность final15 и отсутствие импорта приложений; дрейф исходников, patcher и базовых файлов; существующий/пересекающийся output; неверное сопоставление путей и symlink; изменение входа во время сборки; подмена итогового статуса и потеря вложенного пути; ошибка fsync манифеста. [Результат](evidence/complete17-candidate-result.json).

Серверный PASS final15 от 09:19:55 UTC сохранён как доказательство прежних пятнадцати байтов. Он **не перенесён на final17**. Отдельные проверки внешних writers также не заменяют совместного испытания, реального согласованного окна записи, загрузки изменённого WSGI/CRM, рабочего worker, десяти источников и публикации настоящих карточек.

Активация требует существующих проверенных inode общих и прежних locks, фактического прекращения старых независимых записей и проверенного управления окном обслуживания. Один этот пакет не исключает уже загруженный прежний код. Здесь отсутствуют команды установки, WSGI reload, изменения расписаний, снятия HALT или записи рабочей CRM.
