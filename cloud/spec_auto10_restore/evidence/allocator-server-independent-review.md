# Независимая проверка изолированного серверного теста allocator v2

**APPROVED для изолированного запуска 26 проверок на PythonAnywhere.** Блокирующих замечаний к указанному пакету нет. Это заключение о подготовленном пакете; оно не является результатом его серверного исполнения, разрешением менять production или подтверждением общего Gate B.

Проверены весь `allocator_server_gate.py`, `package_allocator_gate.py`, неизменённые тесты и allocator, пять записей ZIP и соответствие каждого файла манифесту и локальным зафиксированным байтам.

| Артефакт | SHA-256 |
| --- | --- |
| `allocator-server-gate-v2.zip` | `0d5ac01b7e6836e0a66851a6b981a81167f199860592f1d1ada32340bb810d92` |
| `allocator_server_gate.py` | `cc98d9089215d1d0a39e507e7dd731fa5cb26b70f4ea98c4076fdb35ea6d8a80` |
| `allocator_integration.py` | `580e23865e7a128f6f2aace3d6a1c122881c2b1141d13cc56a838e38a7d6ffa7` |
| `car_number_allocator.py` | `620721125f5e6378ac6287fe1f642a6d68b874516a4b51e7955638fd257097e1` |
| `test_car_number_allocator.py` | `7cbc65e9e547e662f633dbda6bf39875f766de9f89b49189d62b957227298ab2` |

Условия исполнения: предварительно проверить SHA всего ZIP; извлечь ровно пять проверенных относительных записей в новый каталог `/home/Carix/spec_allocator_gate_20260909`; запускать точку входа через `python3.10 -I -B`. Манифест внутри ZIP сам по себе не заменяет внешнюю проверку SHA ZIP.

Проверенные границы:

- Родитель читает только три конкретных production-файла `cars_schema.py`, `db.py`, `ai_filter.py` с фиксированными SHA и стандартную библиотеку. Приложения целиком не импортируются; тесты работают с копиями и извлечёнными функциями.
- Чтение исходников использует `O_NOFOLLOW`, ограничение размера, проверку обычного файла и согласованности `fstat`. После тестов повторяются SHA, размер, inode, device и время изменения.
- Рабочий каталог, временные SQLite-базы, блокировки, отчёты и дочерний скрипт находятся внутри нового stage. Сетевые операции и записи вне stage отклоняются.
- Три необходимых дочерних процесса допускаются по точному коду, аргументам, executable, cwd и окружению. Они запускаются с `-I -B`, отдельно проверяют пакет и устанавливают собственный запрет сети, дополнительных процессов и production-чтения. Окружение ограничено `PATH`, `LANG`, `LC_ALL`, `TMPDIR`.
- Исправление v2 декодирует SQLite path/URI через `os.fsdecode(os.fspath(...))` до сравнения и проверки пути. Обнаруженный до серверного запуска сбой обработки bytes устранён.
- PASS требует ровно 26 тестов без ошибок/пропусков, неизменности production-источников, отсутствия импортированных приложений, ровно трёх разрешённых детей и нуля запрещённых операций. Ранний отказ проверки источника/пакета не превращается в PASS.

Ограничения: Python audit hooks ограничивают доверенный проверенный код, а не произвольное нативное расширение. Пакет не проверяет работу загруженного CRM-бота, совместную сборку 15 модулей, production-данные карточек или размещение сайта. Локальный результат автора 26/26 остаётся локальным; фактический PythonAnywhere-результат необходимо получить и прочитать отдельно.

Проверяющий не запускал сервер, не менял рабочую CRM, сайт, расписания или HALT.
