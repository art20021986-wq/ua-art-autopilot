# TASK088 — recovery after handoff v3.2

Status: **BLOCKED BEFORE LIVE LAUNCH. Stage 1 PASS has not been achieved.**

## Owner clarification and current candidate

The owner confirmed in the following turn: CRM means the same Telegram CRM bot.
Adjacent `Цена Украины` / `Цена Грузии` controls and their independent writes are
in scope. The interface question below is resolved; it is retained as historical
diagnostic context, not an outstanding approval request.

The recovery branch now contains a corrected candidate for price selection,
numeric/text/current voice routing, SQLite commits, independent read-back,
whole-cars-table invariants, durable test restoration and consistent backups.
Controls hidden from managers stay hidden. The UA database key and generic UA
automatic parsing path are preserved. Only explicitly selected price input uses
the new guarded price writer.

The owner subsequently answered **«Да разрешай»** to the exact request:
«Разрешаешь для TASK088 использовать штатный защищённый маршрут только для двух
цен в CRM-боте и его базе, сохранив полный запрет изменений сайта?».
This authorizes the existing protected production-credential route for TASK088
CRM source and DB only. Do not ask for that permission again. The website,
publisher and unrelated fields remain outside scope. The original handoff's
false production flag is superseded only for this bounded CRM route.

The candidate is **not deployable yet**. The actual workflow contract rejects
production credentials in `controller_nonproduction`; copying the production
secret under a different name is not permitted. An exact manifest-bound Gate B
authorization can be generated from the owner's permission once the live inputs
exist. No Gate A/B PASS or live acceptance has been invented.

The next infrastructure blocker is the existing protected route's storage gate:
`control_plane._production_storage_probe` requires a SHA-pinned real production
measurement no older than 30 minutes, before the first credential-bearing job.
No fresh measurement exists. Even a GET-only/noop production preflight must pass
this requirement. Do not refresh a historical timestamp, substitute runner or
global NFS capacity for the account quota, or weaken the gate. The task-specific
`owner_preflight.py` is prepared for the owner's PythonAnywhere console to obtain
the missing read-only evidence without using or exporting any token.

Current PythonAnywhere source could not be read: direct browser navigation to
the file was rejected with `net::ERR_BLOCKED_BY_CLIENT`. The plugin directory
returned no PythonAnywhere integration. No current source hash, unpublished
test-car selection or protected site-path snapshot is available. Those inputs
must come from an authenticated read-only preflight before a plan can be pinned.

The installer and controller explicitly refuse live acceptance based on source
strings or isolated callback fixtures. `ui_runtime=NOT_VERIFIED` cannot become
`FINISHED`, and the real `/home/Carix` installer refuses before mutation without
a genuine live acceptance mechanism. Local algorithm tests use temporary roots;
they do not represent a deployed or running bot.

Global workflows, control-plane code, runtime manifest, old approvals/requests,
consumed replay records and the working website/CRM remain unchanged by this
candidate. The held request keeps execution disabled until the measured plan and
runtime verification are ready; this is not an outstanding owner-permission
request. It has no launch marker. Its current dependency hashes replace earlier
candidate hashes listed in the historical notes below.

## Completed recovery

Inspected main at `d6f22b06d345137a29b1e8c2c8014fa553da9165`.

- Run [34592991096](https://github.com/art20021986-wq/ua-art-autopilot/actions/runs/34592991096) failed intake with `AUTOSTART_REPLAY_BLOCKED` for request SHA `0b4edb818dceac55c1605bb2c7e105425c919a8998ae91096b1eb60e0c07bbbe`.
- Earlier run [34591174796, attempt 1](https://github.com/art20021986-wq/ua-art-autopilot/actions/runs/34591174796/attempts/1), job `103237399158`, failed `TOKEN_MISSING` at the beginning of controller execution, before API initialization or any remote writes. Attempt 2 skipped execution; its green overall conclusion does not establish CRM success.
- Closed its exact stale `CLAIMED` record to `FAILED` using the existing `control_plane.py fail ... --terminal` command. No rollback was claimed. Published single-file state update [0e4f53c](https://github.com/art20021986-wq/ua-art-autopilot/commit/0e4f53c51eec341a8b042572ade02013d7e1b948).
- Preserved original request, consumed ledgers, nonce reservations, claim identity and plan. No global control-plane code, safety gates or runtime manifest changed.
- Prepared a candidate request at `tasks/requests/TASK088-GE-PRICE-CRM-STAGE1-R20260911T114500Z.json`, with the same task ID and controller. Its SHA is `cdebee8634c4192ba349f83124b3b37fdc7afe472fbdd831f1361f1bd297e242`. It is not a new pricing system. Candidate execution-contract validation passed, its fingerprint has never been consumed, and no receipt exists.
- Candidate is held on a recovery branch. No new launch marker was created and no live controller was invoked in this recovery session.

Closing a claim does not make its old request fingerprint reusable. Intake rejects previously consumed SHA, nonce or launch path regardless of terminal status. Never delete or rewrite consumed history to allow reuse.

## Historical findings in the original package (not current instructions)

1. **Credential mismatch.** `.github/workflows/uaart_critical.yml`, job `controller_nonproduction`, maps `PYTHONANYWHERE_API_TOKEN` from `secrets.UAART_NONPRODUCTION_PYTHONANYWHERE_TOKEN`. Updating the repository secret named `PYTHONANYWHERE_API_TOKEN` does not populate this different secret. Its current presence was not inspected. Do not bypass the production/nonproduction credential boundary by relabeling a production credential; confirm the authorized target and credential contract first.

2. **No committed price read-back.** `remote_installer.py` commits the schema addition, but its price UPDATE is read through the same connection and then rolled back. It does not prove a committed price write or read-back from another connection.

3. **Independence check can falsely pass.** Before rollback it checks only GE; UA is checked only after rollback. An isolated in-memory SQLite reproduction with an AFTER UPDATE trigger changed UA from 12345 to 7 during a GE write, while both existing checks passed. This proves a defect in the test, not that such a trigger exists in the live DB.

4. **Stage 1/Stage 2 boundary conflicts with the implementation.** The installer edits Telegram labels, the EDITABLE button list and input hints in `/home/Carix/cars_ui.py`, while reporting `stage2_touched:false`. Repository evidence identifies this module as Telegram keyboard/handler code: `tasks/requests/TASK117-REMOVE-CRM-STAGE-BUTTONS.json`, `cloud/task_082_probe/evidence.json`, and `cloud/task_080_price_recognition/evidence/live_gate_a.json`. A distinct current CRM UI has not been identified. Clarify whether Stage 1's CRM interface is the same Telegram interface or locate the separate UI before installing.

5. **Input routing requires current-source inspection.** Historical `catch_message` code can redirect an input containing currency/price words to `price_uah`; `price_georgia` is absent from its field whitelist. The package does not address that path. This is a concrete historical risk, not a verified current live defect.

6. **Rollback and runtime evidence are insufficient.** Source is replaced before DB validation; failure only emits FAIL and does not restore the source. `copy2` is not a consistent SQLite backup under active WAL writes, and the fixed backup directory is overwritten on retries. UI success is inferred from source substrings; no running UI check is made. Site/publisher/Stage2 unchanged flags are constants, not measured evidence.

7. **Remote identity is not fully bound.** Fixed remote script/receipt paths accept only task ID and PASS, without matching remote run ID/request SHA. An Always On trigger can restart a completed script until removed. Correct this within the task package using per-execution binding and an established once-only execution mechanism.

## Minimal continuation order

1. CRM UI/Telegram boundary and protected CRM-only route are owner-authorized. Obtain the fresh read-only owner preflight; this is an access prerequisite, not another approval.
2. Inspect current source read-only. Correct the task-specific package within that agreed scope: preserve existing UA data; commit a controlled test write; read both prices from a separate DB connection; verify independence before restoring; restore with a committed, concurrency-aware operation in `finally`; verify restoration independently. Use unique consistent backups and measured scope/runtime evidence.
3. Add meaningful regression checks for cross-writes, failure compensation and execution identity. Existing package tests check only compilation and substrings.
4. Repin only this package's controller/dependency/test hashes in the candidate request and revalidate. Recompute request SHA; the candidate SHA above is provisional until corrections are complete.
5. After all prerequisites pass, integrate the corrected request/package through the existing repository process. Use the approved protected CRM-only request, exact manifest/approval and fresh storage evidence. Add exactly one fresh `tasks/launch/AUTO-TASK088-GE-PRICE-CRM-STAGE1-<timestamp>.json` in a separate, one-file commit. Bind the final request SHA, a new nonce, current mode epoch, `owner_authorized:true`, `production_allowed:true` for the bounded CRM route, current creation time and a valid expiry.
6. Observe all protected gates through backup, transaction OPEN, `controller_production`, verification and transaction closure. Require actual two-price DoD evidence. Do not report Stage 1 PASS from workflow color, source markers or same-transaction reads. The website remains prohibited without separate owner authorization.

## Acceptance status for this session

- Exact stale claim closure: PASS.
- Consumed history preserved: PASS.
- Fresh candidate fingerprint and execution-contract validation: PASS.
- Live CRM write, DB commit, independent read-back, UA/GE independence and restored test values: NOT VERIFIED.
- Running CRM UI: NOT VERIFIED.
- Live site/CRM/bot mutations by this recovery session: 0; no remote controller invoked.
- **Stage 1: NOT COMPLETE.**

## Final local validation of the revised candidate

- Existing execution_contract validate: PASS.
- Existing execution_contract test: PASS; 82 tests (installer 8, controller 11, DB 11, UI helpers/static routes 44, read-only owner probe 8).
- Dynamic execution was removed from both patcher verification and tests to comply with the existing gate; the gate was not modified. Runtime helpers are ordinarily imported and tested with real SQLite; supplied historical handler source is checked statically.
- UI shape checks remain NOT_VERIFIED for running-bot acceptance. No local test is presented as live CRM PASS.
- Candidate details and exact request fingerprint: candidate_validation.json.
- No launch marker, merge, production secret rebinding or remote mutation was performed.

## Owner-console preflight prepared after route approval

`run_owner_preflight.sh <exact-reviewed-commit>` downloads three hash-pinned
Python modules into a private temporary directory. It runs `owner_preflight.py`
with isolated Python; live CRM modules are never imported. Only sanitized source
structure/checksums, schema, unpublished numeric IDs, guard metadata and capacity
are reported. No raw source, credentials or vehicle price values are exported.
The schema read is WAL-aware; missing existing SHM is refused before connection.
Existing SQLite SHM read coordination may change, but no CRM data is written.

If NFS account quota cannot be queried, the command asks for the current Files
dashboard quota with its exact GB or GiB unit and runs a fresh bounded target
`du` measurement. Decimal GB and binary GiB are handled distinctly. A historical
quota or a global NFS volume cannot supply a gate-ready storage proof. The JSON
report is saved under `/tmp/task088-preflight-result.*.json` and printed for the
owner to return. No live PASS is inferred; source drift/partial measurements are
reported explicitly. A storage proof remains usable for at most 30 minutes under
the unchanged control-plane contract.
