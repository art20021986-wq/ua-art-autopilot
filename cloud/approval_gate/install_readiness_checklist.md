# Чек-лист готовности к включению (одноразовые шаги владельца/админа) — TASK_010

Ничего из перечисленного НЕ выполнено в рамках TASK_010. Это список будущих ручных действий.

## 1. Транспорт в карантин (уже частично существует, но неисправен)

- [ ] Добавить секрет `PYTHONANYWHERE_API_TOKEN` в GitHub Actions Secrets репозитория. Это обязательно — последняя известная синхронизация упала именно из-за отсутствия/пустого значения этого секрета.
- [ ] Добавить переменную `PYTHONANYWHERE_USERNAME` в GitHub Actions Variables.
- [ ] Убедиться, что workflow `PythonAnywhere Inbox Sync` завершается с квитанцией (списком скопированных файлов и их SHA-256), а не молча.

## 2. Генератор манифеста релиза (новый workflow, требует отдельного согласования)

- [ ] Владелец подтверждает архитектуру `Release Manifest Builder` из `github_workflow_install_plan.md`.
- [ ] Разработать и включить сам workflow (отдельная задача, не TASK_010).

## 3. Доверенный шлюз на стороне PythonAnywhere (Gate A/B исполнение)

- [ ] Разместить `pythonanywhere_approval_gate.py` в защищённом, не публичном пути на PythonAnywhere, ВНЕ обычного веб-приложения.
- [ ] Настроить реальные ограничения `ALLOWED_TARGET_ROOTS` и `ALLOWED_COMMAND_IDS` под конкретную инфраструктуру (в текущем виде это референс-заглушки).
- [ ] Определить механизм проверки CPU-квоты через реальный PythonAnywhere API/консоль вместо локального прокси по load average.
- [ ] Настроить каталоги `receipts/` и `backups/` с правильными правами доступа.

## 4. Обычный чат Claude — только чтение контекста (уже доступно, ничего не менять)

- [ ] Подтвердить, что owner понимает: обычный GitHub-контекст в Claude Chat сейчас ТОЛЬКО читает файлы, он НЕ пишет одобрения и НЕ запускает workflow.

## 5. Будущий удалённый MCP-коннектор одобрения (НЕ разворачивать сейчас)

- [ ] Выбрать транспорт (Anthropic remote MCP) и хостинг для `claude_owner_approval_mcp.py`-подобного сервера.
- [ ] Сгенерировать и сохранить `MCP_OWNER_APPROVAL_SECRET` в secret-manager (никогда в git).
- [ ] Настроить OAuth-клиент (`MCP_OAUTH_CLIENT_ID` и связанный secret) для аутентификации владельца, если используется OAuth-путь вместо общего секрета.
- [ ] Подключить MCP-коннектор к Claude Chat владельца как отдельный, явно поименованный инструмент с ограниченной областью действия (только 3 функции: `get_pending_release`, `approve_release`, `reject_release`).
- [ ] Провести отдельный аудит безопасности перед включением этого коннектора в реальном режиме.

## 6. Продакшн-путь (Gate B) — отдельное согласование

- [ ] Владелец отдельно утверждает список продакшн-целей (allowlist) для Gate B, ДО того как Gate B будет реализован в исполняемом виде.
- [ ] Определить процедуру визуальной проверки перед `APPROVE_PRODUCTION`.

До выполнения перечисленного: `PYTHONANYWHERE_SYNC_ACTIVE = NOT_PROVEN`, `GATE_A_ACTIVE = NO`, `GATE_B_ACTIVE = NO`, `ORDINARY_CLAUDE_CHAT_APPROVAL_BRIDGE_ACTIVE = NO`.
