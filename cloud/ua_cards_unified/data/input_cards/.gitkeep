# Intentionally empty.
# Place real card JSON files here before running the pipeline.
# Do NOT place synthetic placeholder cards with ids UA-0001..UA-0008 here;
# the preflight and runner will reject them (BLOCKED), by design.
