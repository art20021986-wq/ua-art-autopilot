# PREVIEW FIXTURES — NOT PRODUCTION EVIDENCE

Everything in this directory is a deterministic, locally-generated illustration produced by the pure render functions in `START_UA_CARDS_UNIFIED.py`. None of it was served over HTTP, none of it was visually checked in a browser, and none of it proves anything about the live UA ART site. It exists only to show reviewers what the candidate output looks like structurally.
