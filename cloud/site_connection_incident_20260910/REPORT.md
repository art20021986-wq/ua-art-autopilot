# Повторные ошибки соединения UA ART — 10.09.2026

Статус: монитор установлен и запущен после команды владельца «Загружай». Задача 270984 — Running; первый серверный отчёт healthy (10:09:33 UTC). Причина отказа на телефоне не установлена.

## Установка завершена
Файл загружен через штатную форму PythonAnywhere. Создана ровно одна новая Always-on задача с указанной ниже командой. Существующая задача start_safe.py сохранена. Первый latest.json создан и прочитан через редактор: status=healthy, exit_code=0. Подробности — evidence/deployment-receipt.json. Долгосрочная стабильность и восстановление Chrome/TikTok пользователя этим результатом не подтверждаются.

## Выполнено
- Один перезапуск точного веб-приложения www.uaart.com.ua штатной кнопкой PythonAnywhere. Кнопка вернулась в активное состояние.
- После операции браузер открыл основной адрес с перенаправлением на главную и затем каталог.
- [Диагностика DNS/TLS/HTTP](https://github.com/art20021986-wq/ua-art-autopilot/actions/runs/34462873650): 12/12 IPv4-запросов HTTP 200, включая заголовки iPhone Chrome/WebView. Это не эмуляция реального телефона/TikTok.
- [Проверка готового монитора](https://github.com/art20021986-wq/ua-art-autopilot/actions/runs/34463620597): 18 тестов PASS и 4/4 реальных HTTPS-маршрута healthy в 09:59 UTC.
- Сайт без www обслуживает перенаправление через отдельный сервер; www указывает на CNAME PythonAnywhere. Сертификаты обоих адресов прошли проверку. AAAA не найдены; отказ curl -6 сам по себе не доказывает причину мобильного сбоя.
- Изменений данных CRM, карточек, медиа, DNS/TLS и исходников сайта не выполнялось. main и аварийный HALT автопилота сохранены.

## Выполненная установка
Файл: `uaart_connection_monitor.py`
SHA-256: `989acd5d3ff1ce2dc6a49810a96b4451d63d89faf558ec96da3726d369f276fd`
Назначение: `/home/Carix/uaart_connection_monitor.py`
Одна новая Always-on task:
```bash
python3.10 -I /home/Carix/uaart_connection_monitor.py --watch --interval 300 --evidence /home/Carix/uaart-monitor/latest.json
```
Она проверяет четыре публичных HTTPS-адреса, ждёт 300 секунд после цикла, повторяет неуспешную проверку не более трёх раз с паузами 15 секунд. Каждый сетевой процесс ограничен 20 секундами. Проверяются сертификат, конечный адрес, HTML и название UA ART. Используются только стандартные библиотеки Python.

Сохраняются фиксированные latest.json и last_incident.json, плюс файл блокировки. Второй процесс с той же папкой отклоняется. Монитор не использует токены хостинга, не перезапускает сайт, не меняет сайт и не отправляет сообщения. Он улучшает обнаружение и фиксацию сбоев, но не гарантирует доступность и не устраняет ещё не установленную причину.

Откат установки: отключить только новую задачу мониторинга. Существующий start_safe.py и процессы CRM не затрагиваются.

## Блокировки
1. Политика Cloud Browser запретила прямой просмотр error.log. Обход не выполнялся.
2. Автоматическая проверка отклонила загрузку скрипта в файловую систему PythonAnywhere: требуется явное подтверждение точного файла и назначения. Исторический отказ снят новой прямой командой владельца «Загружай»: разрешённая загрузка выполнена, задача запущена.
3. Изменять исходный монитор GitHub отдельно нельзя: его расписание и хеши закреплены действующим runtime. Подготовленная установка в отдельный Always-on процесс не снимает и не меняет эти ограничения.

## Что остаётся
- Получить подтверждение восстановления именно в Chrome и TikTok пользователя; ответа после перезапуска пока нет.
- Установка проверена: задача Running, первый latest.json healthy. Дальнейшие циклы выполняются с паузой 300 секунд после завершения предыдущего; сам монитор не гарантирует восстановление соединения с телефона.
- Установить причину сбоя по допустимым данным сервера и результатам соединения с телефона. Автоматические повторные перезапуски до этого не включать.

## Продолжение диагностики — 10:16 UTC
После нового сообщения владельца проверены также оба HTTP-входа. [Run 34465114314](https://github.com/art20021986-wq/ua-art-autopilot/actions/runs/34465114314): 18/18 IPv4-запросов по шести URL с тремя User-Agent завершились HTTP 200, с проверкой HTTPS-сертификатов на конечном соединении. HTTP apex перенаправляет непосредственно на HTTPS главную; HTTP www — через HTTPS корень. Петля редиректов не обнаружена. Это не воспроизводит фактическую мобильную сеть пользователя.

Второй серверный цикл монитора также healthy, check_id 2a36daf86dfd4afebab379ccfa02d789, started_at 2026-09-10T10:14:35.119+00:00. Процесс выполняет повторные циклы.

Причина клиентского ERR_CONNECTION_FAILED остаётся неустановленной. Для следующего диагностического шага нужна проверка прямой HTTPS-ссылки в Chrome через другую сеть (Wi-Fi относительно 5G на скриншоте). Без этого нельзя обоснованно выбрать изменение маршрутизации или сервера. Новые изменения production в этом продолжении не выполнялись.
