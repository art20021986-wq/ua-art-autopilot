# TASK 043 — Response to the ten TASK 042 defects

| # | TASK 042 defect (independent audit) | Correction in TASK 043 |
|---|---|---|
| 1 | `apply_transform("Автомобиль в море уже 20 дней")` corrupted grammar via case-insensitive substring replace. | `transform.apply_transform` now only acts when the **entire stripped string** equals a registered anchor. Embedded substrings inside longer sentences are never touched. Covered by `test_ordinary_prose_car_in_sea_unchanged`. |
| 2 | `"Море волнуется, паром в порту"` was corrupted by blanket regex. | Same whole-string-only rule; covered by `test_ordinary_prose_sea_is_rough_unchanged` and `test_ordinary_prose_rest_at_sea_unchanged`. |
| 3 | Legacy JS input alias `"В море"` inside `<script>` was rewritten. | `transform_html` tracks a script/style skip-stack and never modifies data inside those tags, regardless of content. Covered by `test_script_body_untouched` and the JS-legacy-alias case in `test_determinism_10_runs`. |
| 4 | `gate_a_preview.py` invented the same stage `sea` for all nine cards. | This round does not invent any stage. `gate_a.build_gate_a` only transforms whatever real HTML it is given; it never assigns a stage. Real per-card stage evidence must come from a real `discover.py` run against real files (not yet executed — see honest finish state). |
| 5 | `discover.py` scanned an arbitrary root recursively (up to 5000 files), followed symlinks, had no allowlist, no no-follow reads, no TOCTOU proof, no redaction, no dedup, no classification. | New `discover.py`: hardcoded bounded `CANDIDATE_REGISTRY` (no recursion), `root == allowed_root` enforced, `lstat_check` rejects symlinks/non-regular/multi-hardlink files, `safe_read` uses `O_NOFOLLOW` where available and compares `lstat`/`fstat` identity, `redact_line` removes secret-shaped lines, `seen` set deduplicates occurrences, and `classify_text` classifies every occurrence into the required categories. |
| 6 | `transform.py` applied global regex to arbitrary source text, not parsing HTML or distinguishing contexts. | New `transform_html` structurally parses HTML with `html.parser.HTMLParser`, only editing visible text nodes and `data-ru`/`data-uk` attributes; script/style/other attributes are reproduced byte-for-byte. |
| 7 | `contains_forbidden` used substring matching, conflating visible labels and prose. | Replaced by `classify_text`, which requires an **exact whole-string match** after stripping to classify something as `USER_FACING_STATUS`/`USER_FACING_SHORT_STAGE`; anything else is `ORDINARY_PROSE` or `LEGACY_INPUT_ALIAS_CANDIDATE`. |
| 8 | Gate A used non-atomic writes with no allowlist roots, hashes, rollback, or manifest binding. | New `gate_a.py` validates every destination against the three approved task-043 roots before writing, uses temp-file+fsync+`os.replace` atomic writes, tracks and rolls back every file written in the current call on failure, and records `sha_before`/`sha_after` plus a diff and occurrence list per file in a receipt. |
| 9 | No real candidate patch for any canonical source was produced. | Still true and stated honestly: no real canonical source was available to this round. `gate_a.py` explicitly marks any non-`.html` candidate as `AMBIGUOUS: no reviewed per-file transform for this source type in this round` rather than guessing, satisfying the "ambiguous or multiple anchors BLOCK; never guess" requirement. |
| 10 | `UA0009_SAFE_TO_PUBLISH=NO` must remain NO until real evidence passes. | Unchanged: **NO**. No claim of publish-readiness is made in this round. |
