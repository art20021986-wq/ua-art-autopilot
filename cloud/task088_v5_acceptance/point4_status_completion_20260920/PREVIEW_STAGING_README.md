# Exact candidate Preview: подготовка и отдельное переключение

Ничего удалённо этим агентом не запускалось. Операции ниже предназначены только для существующего отдельного Preview `https://carix.pythonanywhere.com`; рабочий сайт `www.uaart.com.ua` не переключается.

Свежий root readback подтвердил dedicated WSGI `/var/www/carix_pythonanywhere_com_wsgi.py` SHA256 `9b9578fe30e233cea6e46678da60dd360cae1861fca2723c08e19272bc75b555` и прежний private config SHA256 `ab4b13e5f6c2a0f05801a6e89891b38474852c6cfc3db229f9fff72dabcca5ee`. Config path: `/home/Carix/autopilot_inbox/cloud/task088-v5-preview-stage-zlcw0vma/gallery-preview-j6b3l4hd/config.json`. Действует прежняя политика `PUBLIC_READ_ONLY_PREVIEW_OWNER_AUTHORIZED`; её значение переносится без расширения доступа. Root отдельно проверил Python3.10, HTTPS и отсутствие static mappings у dedicated app.

1. После реконструкции полного нового capture использовать только terminal `summary.json`: status `PASS_CORE_DOUBLE_READ_STABLE_OBSERVATION`, `export_completed:true`, все10 stability flags true. Pre-export `observation_summary.json` не заменяет terminal result.
2. Собрать final installation candidate из фактических current inputs; завершить source/HTML checks и независимый review. `check_current_preview.py` сверяет все48 HTML и создаёт affectedmatrix, не Gate.
3. Локально `python -I -B package_exact_preview.py --candidate <directory> --candidate-manifest <exact-manifest-json> --observer <terminal-summary-json> --repository <repo> --output <new-zip>`. Проверяются SHA всех файлов finalcandidate. В ZIP попадают только48 HTML, пять проверенных public Preview runtime modules (четыре прежних; wsgi_preview.py с узкой CSP-коррекцией) и manifest. Private Python, DB и config не включаются.
4. Доставить helper и public ZIP в существующий private inbox. `stage_exact_preview.py --self-sha256 <helper-sha> stage` требует уникальный operation-id, package+observer paths и SHA, существующий config path и свежие ожидаемые WSGI/config SHA. Все параметры перечислены в `--help`. Выполнять `python3.10 -I -B`. По умолчанию hard time bound180s, допустим явный bound до300s; превышение означает неполный результат, а не PASS.
5. Stage создаёт только новый `/home/Carix/autopilot_inbox/cloud/pr114-preview-<operation-id>` (0700), files0600. Старые bundle/config/runtime сохраняются. Собирается manifest из уже готовых finalcandidate HTML; новый builder не рендерит их заново. Diagnostic/info/podbor HTML копируются только из текущего наблюдённого allowlist. Referenced assets остаются под explicit `/home/Carix/video`, но доступны исключительно по manifest routes с SHA/bytes. Нет directory fallback. Их реальные bytes хэшируются дважды. Проверяются точные reviewed runtime pins, deny private routes и прежняя access policy. Stage receipt: `01-stage-receipt.json`; browser/reload/PreviewPASS не заявляются.
6. Root проверяет stage receipt и его SHA. Отдельно `stage_exact_preview.py --self-sha256 <helper-sha> switch --work <new-work> --stage-receipt-sha256 <actual-sha>` повторно проверяет bundle/resources/config/current old WSGI, сохраняет durable `02-switch-intent.json`, затем один `os.replace` только dedicated WSGI. `03-switch-receipt.json` содержит фактический outcome. Это cooperative compare-before-replace, а не атомарный OS-CAS; concurrent foreign writer недопустим.
7. После любого обрыва сначала `stage_exact_preview.py --self-sha256 <helper-sha> inspect --work <work>`: сравнение реальных dedicated WSGI bytes с old/candidate. Существующий switch-intent блокирует повторную запись даже если old bytes всё ещё видны. Никакого автоматического повтора или rollback неизвестного результата. Старый production WSGI проверяется как неизменный.
8. Root выполняет штатный reload только dedicated app через PythonAnywhere Web UI и проверяет реально обслуживаемый новый manifest/runtime/page binding. Helper никогда не делает reload/API/HTTP calls.
9. В `/__uaart_preview__/viewport.html` провести вычисленную affectedmatrix; новые7основных страниц дают минимум42 cases. Отдельные top-level ссылки/диагностики, filters/counters и gallery interactions не заменяются in-process проверкой. При ошибке ресурса корректный результат — failure, не подмена внешним URL/ослабление CSP/manifest.

Helper сохраняет durable stage/switch intents, текущие ошибки не перезаписываются. При частичном stage существующий work повторно не используется; сначала inspect/files readback. Все созданные receipt являются receipts изолированного Preview и не являются Stage3/Stage4/GateB/production installation receipts.

CSP-коррекция: runtime wsgi_preview.py SHA256 `191989bd07d0eea55782671778b8bb2dd1b531eda88a561bbc4b229afcd7d289`. Только home/catalog получают connect-src exact same-origin `/video/katalog.html`, если он включён в manifest. Остальные прежние endpoint/auth/method/byte boundaries сохраняются; карточки и harness остаются connect-src none. Две новые scoped проверки PASS; реальный browser fetch ещё не наблюдался. Stage helper SHA256 `dad2b41e7132a0c3f93429cda2b9d5d5b0ac55b57733c8343160780c7503afdb`.
