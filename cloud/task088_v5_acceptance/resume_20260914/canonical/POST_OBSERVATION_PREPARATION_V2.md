# Canonical preparation after the actual installation observation

This is a dependency checklist for the already authorized PR114 release. It
creates no Gate, owner approval, claim, transaction, launch marker or live PASS.
The exact executable construction order remains in CONSTRUCTION_ORDER.md.

The v2 gallery builder is
`ce6fb512f2b2c5dadb81c6412b1da28a43b8ba0d8b9d325f93c03833d9368cc9`.
Actual offline results are446/446 price tests and183/183 maintenance tests under
source manifest
`0dae6071812640061367ef54dedb52a4b6a8e155449d526202f2496f52b55f38`.
The first real gallery refresh stopped before changing Preview WSGI. Its
`EXPLICIT_LITERAL_GALLERY_REQUIRED` receipt remains historical failure evidence.
A later successful build must have its own actual receipt and candidate hash.

## Actual completion snapshot

ObserverV3 is actually complete: `server/install_1650.json`,16:50:36UTC,
9625files and986727605bytes including17 reviewed hidden files. Actual private
preflight completed17:01:04UTC: candidate verification PASS, offline schema/CRM
and audit unchanged, protected system readback PASS. Top-level status remains
BLOCKED only by `CANONICAL_CONTROL_BRIDGE_GATE_B_AND_ACTIVATION_NOT_RUN`.
The original report must retain that status. Installation candidate has54files
and manifest `eaceee78bcdbabc03e2430fd096c921d19ca1942e62fb5aa528dbf57a3eb8611`.

`PREFLIGHT_BROWSER_BINDING_V2.json` compares all20 served preflight page hashes
with retained browser/harness hashes:20/20 match. Observer and preflight DB,
schema and entire inventory also match. This is an evidence comparison, not a
new browser run. The Preview resource manifest `c6febae073064d66c89a553a1d23538c8dc71c40c9737c440d33a3ba457611ca`
and installation candidate manifest above serve different contracts; never
substitute one for the other.

The helper accepts actual preflight candidate/offline/protected-readback PASS
fields; it does not require changing the overall BLOCKED status to PASS. It still
requires genuine full Preview, current authority and fresh quota/writer inputs.
Full-size photographs and owner-supplied read credential remain the missing
manual inputs. The exact16:48 presence observation found the token path absent.

## Work that can become concrete after a valid observation

1. Retain the actual observation bytes, SHA256, start/end time, complete scoped
   file inventory and canonical database tuple. Check the original observer and
   installation engine hashes, account quota provenance and expiry. Neither
   `HIDDEN_SOURCE_REQUIRES_REVIEW` nor
   `HIDDEN_PUBLIC_ENTRY_REQUIRES_REVIEW` is a completed observation. Review only
   necessary metadata for the unknown entry before changing an explicit
   inventory policy; do not read private contents merely to suppress a failure.
2. Use existing `build_preflight_bundle.py` with this real observation, the
   immutable Stage2 receipt and actual routing proof. Keep candidate private
   source files outside git. Run the reviewed private preflight in a new staging
   directory and retain actual source/candidate/inventory/database hashes. A
   preflight may reveal further real blockers; report them without creating
   placeholder fields.
3. Check whether the exact flat installation release still matches its source
   closure. The current gallery builder change is Preview code; do not rebuild
   or rerun unrelated suites solely because a report timestamp is older. If any
   release source changes, freeze and verify that actual changed candidate.
4. Compare the final Preview build with retained browser page/source bindings.
   Reuse byte-identical home/catalog/cards and their completed120CSS,18card+
   18diagnostic, language/filter and counter evidence. Record newly added media
   separately. A valid file pin cannot close the actual full-size-photo gap.
5. Prepare the real proposed delegation and request/manifest scope from actual
   task identity, authority, protected paths and observed input hashes. The
   current main/mode/runtime/activation and required workflows must be freshly
   read before final registration. Values recorded in READINESS.md are audited
   historical baselines, not permission to assume the remote branch stayed put.

## Work that remains gated by actual full Preview evidence

`derive_candidate_parts.py` requires all seven real inputs: preflight, preview,
quota, writers, routing, delegation and software. Its refusal while any input is
missing is expected. Do not invoke it with invented PASS documents or relax its
requirements to obtain output. Once inputs exist, use default dry-run first;
optional parts go into a new private local directory.

For the `software` input, the current executable contract is the actual
`software/gallery_fix_v2/PUBLIC_GATE.json`
(`UA-GE-PRICE-PROTECTION-SOFTWARE-1`). Keep the446-test aggregate and private95
logs as supporting evidence; the aggregate report has a different schema and
must not be renamed to masquerade as the public gate contract. Likewise, this
analysis map is not the required `TASK088-FINAL-V5-PREVIEW-GATE-1` report.

Full Preview must still satisfy every actual24 criterion and exact current
candidate/source/DB binding. Photo display is NOT_PASSED while the permitted
observation is missing. The explicit Cloud browser policy denial must not be
retried through another tool, browser route, proxy or indirect execution. Keep a
concrete manual acceptance handoff if that is the only permitted route.

After a real full Preview evaluation, complete the canonical manifest/Gate and
the representation of the existing owner authorization. Check real timestamps
and evidence freshness at execution time; changing `evaluated_at` alone is not
fresh verification. Preserve the current three-pin runtime registration route
and validate complete final artifacts. The launch must remain its separate
single-parent, one-new-file marker commit. Only the canonical workflow creates
claim, transaction and nonce records. `prepare_install_plan.py` runs after those
actual records exist.

## Stage4 boundary and durable continuation

The continuous authenticated server reader remains a Stage4 requirement; the
15:34 credential-absence observation is historical and no secret value was read.
Preview evaluation itself needs no reader credential. Continue through the
approved private credential/configuration route and real live acceptance after
installation; do not substitute chat connector access or copied timestamps.

On resume verify checkpoint archive hashes, then read current Git main/head,
mode/HALT/delegation, actual server Preview configuration, current source/DB and
remaining quota. Preserve prior failures, failed staging directories, WSGI
backup references and raw observations. Never rerun an expired kit command or
assume an earlier unfinished attempt changed WSGI. Existing publication
authorization persists; complete evidence and canonical controls without
requesting that same permission again.
