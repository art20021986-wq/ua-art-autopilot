# FINAL v5 actual installation preparation

Status: code and isolated verification only; no live installation, activation,
launch marker, canonical approval or completion receipt was produced.

The existing Stage 2 controller handles only its already closed CRM installation.
It must not be launched again. A fresh Stage 3 identity and actual canonical
execution adapter are required for the larger source/schema/site bundle.

## Read-only commands

On the server, put reviewed `observe_install_inputs.py` and `install_package.py`
in a new private task directory. Supply the current authenticated account quota
JSON, containing `source: PYTHONANYWHERE_AUTHENTICATED_ACCOUNT`, `account: Carix`,
aware `observed_at`, integer `used_bytes` and `limit_bytes`.

```sh
python3.10 observe_install_inputs.py --engine install_package.py --engine-sha256 ACTUAL_ENGINE_SHA256 --quota-json ACTUAL_QUOTA_JSON
```

For a hash-only read before exact quota is available, omit `--quota-json`; the
result explicitly says `OBSERVED_QUOTA_PENDING`, which cannot be used to build
or install a package. This read makes no private backup and requires no disk
quota decision.

Save its hash/count-only stdout as a private observation JSON. The script hashes
all root Python/web files and the complete video/site trees, reads the current
published set and schema, and performs a separate-connection DB readback. It does
not copy DB rows, expose price values, import live modules, or write the server.

Locally, build the exact package from that observation and the existing canonical
Stage 2 receipt. No hardcoded source hashes, published counts or quota are used.

```sh
python3 cloud/task088_price_sync/build_preflight_bundle.py . --observation ACTUAL_OBSERVATION_JSON --stage2-receipt state/receipts/TASK088-GE-PRICE-CRM-STAGE2.json --output-directory PRIVATE_OUTPUT_DIRECTORY
```

Stage the content-addressed archive into a unique private directory named
`/home/Carix/autopilot_inbox/cloud/task088_price_sync_<unique-id>` and validate
its archive/bundle hash. `stage_preflight.py` can instead retrieve an exact
reviewed repository commit with `--staging-id UNIQUE_ID`; it requires the v5
`preflight_bundle.json` to exist at that commit and never overwrites a directory.
Run the package's preflight in that directory:

```sh
python3.10 preflight.py --output-id preflight-UNIQUE_OBSERVATION_ID --expected-bundle-sha256 ACTUAL_BUNDLE_SHA256
```

The private report/candidates and two SQLite copies remain private. Candidate
PASS is limited to source/price-fragment/schema checks and is not full Preview
PASS. An overall BLOCKED preflight exits nonzero even when its candidate checks
pass, so automation cannot confuse partial success with a production Gate.

## After actual full Preview PASS and canonical claim creation

Retrieve the actual raw canonical documents into a private evidence directory:
`request.json`, `claim.json`, `transaction.json`, `gate_b.json`, `stage2.json`,
`quota.json`, `writers.json`, `manifest.json`, `owner_approval.json`,
`preview_gate.json`. None is generated by preparation tools. Gate B must bind the
actual full Preview report, the manifest and the actual writer fence report.

```sh
python3.10 prepare_install_plan.py --engine install_package.py --engine-sha256 ACTUAL_ENGINE_SHA256 --preflight-report ACTUAL_PRIVATE_REPORT --candidate-root ACTUAL_PRIVATE_CANDIDATE_ROOT --evidence-directory ACTUAL_CANONICAL_EVIDENCE_DIRECTORY --output-directory UNUSED_PRIVATE_PLAN_DIRECTORY
```

This command validates the evidence, copies exact candidates/evidence to private
immutable preparation, and returns its plan SHA. It does not execute the plan.
The trusted execution adapter must independently retrieve current canonical
state immediately before calling `install_package.install(...)`, use the actual
reviewed runtime `install` function, and supply the returned expected plan SHA.

## Canonical installation adapter

`controller.py`, `backup_controller.py`, `rollback_controller.py` and
`remote_adapter.py` now implement the scoped installation child for the existing
CRITICAL workflow. The workflow retains ownership of canonical PREPARING, OPEN,
ROLLING_BACK and final reconciliation; these adapters do not create policy,
claims, authorization, nonces or launch markers.

PREPARING creates a real full website/source/DB backup while retaining the
publication lock and a DB writer transaction. OPEN validates the exact Preview,
Gate B, current claim, private candidates, source/dependency/DB hashes and backup
bytes before calling the reviewed engine. A separate remote verification checks
installed source, schema, protected HTML and unchanged CRM data. Provider static
routing is independently read before and after each operation so a changed
hosting mapping cannot be hidden by unchanged WSGI source. The known unserved
`site/index.html` stays byte-identical under the hash-bound routing evidence.

Rollback restores only this transaction's candidates and empty added schema;
newer operator prices, foreign writes and nonempty operation/audit tables are
preserved. The complete backup DB, schema and every saved media/source file are
verified before execution or restore. The controller cleans up only its own
identified temporary runner and never restarts the existing CRM supervisor.

Use `python3 -B cloud/task088_v5_install/build_release.py
--refresh-reviewed-code` after the reviewed source is frozen. It builds the flat
package required by `automation/execution_contract.py`, containing only public
repository code and its source hashes. No captured server sources or CRM rows are
included. A real request/manifest must pin the resulting exact package.

The remaining live prerequisites are full protected Preview acceptance and its
canonical request, claim, Gate B, fresh quota and writer-fence evidence. The
installation receipt explicitly keeps Stage 3/4 incomplete and activation,
public acceptance and Telegram acceptance unperformed. Provisioning the private
v2 binding and activating the runtime belong to the subsequent reviewed live
acceptance work; installation does not imply activation.

The required source closure includes `uaart_price_control_reader.py` in the
preflight archive, immutable candidate manifest, remote package, installed code
pins and release. Missing reader code is an installation failure even if a
partial manifest is rebound. Installing the module never creates its private
configuration, credential, delegation or activation anchor.

The reader is a separate continuous producer. After the real Gate and explicit
binding evidence are installed, its reviewed Always-On command is:

```sh
python3.10 /home/Carix/uaart_price_control_reader.py --config ACTUAL_PRIVATE_READER_CONFIG --config-sha256 ACTUAL_CONFIG_SHA256 --loop
```

Its authenticated token file stays private with mode 0600; its configuration and
cache paths are hash-bound. Activation must verify the exact reader supervisor
and the existing CRM supervisor as Running, read back a fresh authenticated
control observation, and check that the consumer accepts the same reader/config
pins. Restart recovery must include both processes. A configured or running
reader alone does not close Stage 4, and a missing/stale observation blocks price
publication. No extra paid slot or new credential is provisioned by these tools.

After actual installation, run post-deploy public/CRM verification, controlled
process activation, actual queue/restart/Telegram acceptance and persistent
UA/GE PRICE PROTECTION before any Stage 3/4 CLOSED receipt.

## Local verification

```sh
python3 -m unittest discover -s cloud/task088_price_sync -p test_install_package.py
python3 -m unittest discover -s cloud/task088_price_sync -p test_v5_install_preparation.py
PYTHONPATH=cloud/task088_price_sync python3 -B -m unittest discover -s cloud/task088_v5_install -p test_adapter.py
```

Installer safety, immutable preparation/observation and adapter tests use
isolated temporary roots. The adapter suite additionally checks complete backup
integrity, phase binding, read-only provider routing and preservation during
rollback.
They use TEST evidence only. They verify full backups,
separate DB readback, dynamic vehicle sets, whole-system drift, source/schema
rollback, foreign write preservation, strict full Preview binding, private file
modes, and refusal to substitute or recreate canonical evidence.
