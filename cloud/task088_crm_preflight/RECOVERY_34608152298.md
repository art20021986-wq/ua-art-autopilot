# TASK088 diagnostic run 34608152298: evidence and recovery boundary

Status: **FAILED; CRM price acceptance NOT_PERFORMED**.

The owner-authorized TASK088 storage override is merged in PR #102, merge
`d754351c090fbcc7a015a817c3c8d456e8360ccf`. Global replay protection remains enabled.
The launch commit `b3e34208482ae5e89e5581ece7f3408ae527fcf2` changed exactly one marker.

Run: https://github.com/art20021986-wq/ua-art-autopilot/actions/runs/34608152298

## Observed results

- Context, intake, persistence, request validation, planning, critical validation,
  preparation, credential-backed source snapshot, and transaction OPEN passed.
- The controller failed at 2026-09-11T14:13:59Z after approximately 285 seconds.
  Its stdout reported only FAIL and CRM acceptance NOT_PERFORMED. The workflow
  did not upload controller evidence after this failure. The actual controller
  exception and its cleanup result therefore remain **UNKNOWN**.
- A timeout is consistent with elapsed time but is **not an established cause**.
- Rollback reservation passed. The rollback execution job failed at
  `PINNED_SOURCE_COMMIT_BINDING`, before the credential-bearing rollback step.
- The recovery job persisted an emergency HALT and BLOCKED_ROOT_CAUSE claim.
  The transaction remains ROLLING_BACK. No successful rollback receipt exists.

## Confirmed rollback defect

The rollback job selected `needs.validate.outputs.source_commit`,
`5bddc53af1fe8448e9c0bcd7782471db1da2eeb3`, a later intake state commit. Its guard
correctly required the claim's original `autostart_source_commit`,
`b3e34208482ae5e89e5581ece7f3408ae527fcf2`.

The draft changes only this job's SOURCE_COMMIT selection to
`needs.validate.outputs.workflow_source_commit`. It preserves the actual binding
checks and leaves forward execution's source selection unchanged.

## Required evidence before reconciling the failed run

The controller package permits only source GETs and bounded files in this exact
temporary diagnostic directory. Its business target manifest contains two noops.
It does not implement CRM/database/site writes or a restart of the existing bot.
However, this design restriction alone does not prove successful cleanup of the
new temporary AlwaysOn task.

Exact remote directory:
`/home/Carix/autopilot_inbox/cloud/task088_crm_preflight/runs/34608152298-062ad76555efb682b5c5835c74193bfed5783db2f2749312aefa425be8b8581a`

Read-only reconciliation needs the existing `plan.json`, `trigger.json`,
`started.json` and `result.json` (or verified absence), the current status of the
task with that exact command and description, and source hashes matching the
backup snapshot. Logs must be restricted to that newly created task and sanitized.
Do not import live CRM modules or export secrets/source/vehicle values.

The backup snapshot's canonical SHA-256 is
`6b922c9656d46fafdf8c51f82cf3cb4fa512b4db82e4cf1113429b7d5c1ff34b`.

Preserve the consumed ledger, nonce, claim, transaction and HALT. Do not reset
ROLLING_BACK to OPEN or rerun the consumed automatic rollback. Existing
`state/reconciliations/*no-production-write.evidence-v2.json` documents explicit
manual reconciliation with truthful semantic outcome; it must not be copied as
proof of facts not observed in this run.

## Draft activation boundary

This corrective draft has no launch marker, replacement request, runtime
activation or HALT mutation. Its controller hashes and workflow blob differ from
the consumed request's pinned package/runtime. It must not be merged as an active
launchable package without a consistent reviewed runtime activation and fresh
request identity after reconciliation. Local regression tests are not live CRM
acceptance.

The existing control-plane security verifier also pins the write workflow's
exact SHA internally. It correctly rejects this unregistered draft as
`WRITE_WORKFLOW_EXACT_SHA256_MISMATCH:.github/workflows/uaart_critical.yml`.
Updating only the external runtime manifest is therefore insufficient; the
reviewed registration must keep both checks consistent, without disabling either.
