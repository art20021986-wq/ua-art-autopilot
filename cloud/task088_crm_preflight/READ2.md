# TASK088 automatic file reader

The owner asked for automatic execution because pasting a Bash block on the
phone was impractical. READ2 runs in the existing protected GitHub Actions route
and permits only PythonAnywhere file GET requests. It uploads no remote code,
creates no AlwaysOn task, and does not execute Python on the target account.

It reads the exact prior run 34608152298 plan/trigger/started/result files and
the log identified by its verified trigger record. It exports only safe log
metadata, then reads current CRM source hashes and AST shape without executing
the source. Missing prior evidence is reported as absent. A successful reader
receipt means successful data collection, never successful price installation.

The failed prior read-only attempt is reconciled separately. Its pinned code
contains no business mutation operation, and the owner's IMG_8924 screenshot
shows both current AlwaysOn entries with no TASK088 entry. The reconciliation
records the scope and limitations explicitly: original error unknown, current
source/DB readback not yet performed, diagnostic files may remain, no rollback
was executed. ROLLED_BACK is the existing terminal metadata representation;
the semantic result is ABORTED_READ_ONLY_DIAGNOSTIC and the claim is FAILED.
Its exact HALT is archived. Consumed requests, launch marker and nonce survive.

The future rollback job's SOURCE_COMMIT is corrected to the original launch
commit. The exact workflow SHA and runtime activation are registered together;
all existing checks remain. Original mode policy and storage waiver scope are
preserved. READ2 has fresh request/approval/gate/receipt paths and a fresh nonce.

Validation: 10 reader tests passed through execution_contract; 13 storage-scope
tests and the functional rollback identity regression passed. The broader
workflow suite has the same seven failures against both the unchanged baseline
and this candidate (34 common tests); those unrelated historical assertions
were not changed. Full runtime verification passes for the registered candidate.

CRM price acceptance: NOT_PERFORMED. No Stage 1 or Stage 2 completion is claimed.
