# SEO-KOREA-KYIV-001 — отчёт о подготовке (round 1)

TASK_ID: task_102
ISSUE: #36

## Статус исполнения на момент этого ответа
Исполняемый файл `seo_korea_kyiv_001.py` создан и структурно проверен (ручной построчный разбор синтаксиса и логики). Файл **не запускался** против живого `/home/Carix` и живого `https://www.uaart.com.ua` в рамках этого раунда — согласно тексту задачи, фактический триггер на GitHub-раннере и на PythonAnywhere выполняется отдельно, после ревью кода.

## Почему не запущено сейчас
У Claude/Cloud-воркера в этом раунде нет прямого сетевого доступа к PythonAnywhere API токену и к продакшн-файловой системе `/home/Carix`. Задача прямо требует: "Never touch UA ART production directly from Claude/Cloud" и "GitHub-side triggering/review will be handled separately". Поэтому предоставлен готовый, безопасный по конструкции файл, а не заявление о выполненном прогоне.

## Что подтверждено статическим анализом
- Единственный файл, без внешних pip-зависимостей (только стандартная библиотека Python 3).
- Оба режима (runner / PythonAnywhere) реализованы и разделены по автоопределению контекста.
- Все обязательные шаги пайплайна присутствуют в правильном порядке: QUEUE → BACKUP → SANDBOX → GATE A/B → PRODUCTION → GATE C/D → ROLLBACK-если-нужно → FINAL REPORT → archive self.
- Baseline (VIN/стадии/количество карточек) вычисляется динамически сканированием реальных файлов, а не хардкодом 13 или 16.
- Ни один путь, кроме новых `/video/seo/ru/*.html`, `/video/seo/ua/*.html`, `/video/sitemap.xml`, не записывается.
- Gate A выполняет реальные HTTPS-проверки `/robots.txt`, `/sitemap.xml`, `/` и блокирует релиз с точной причиной, если корневой robots.txt недостижим или локальный источник для него не доказан — никаких изменений WSGI не производится.
- Rollback восстанавливает изменённые файлы побайтово из backup и удаляет новые файлы, если Gate C/D не прошли.

## Что ещё НЕ подтверждено (честно, без выдумывания доказательств)
- Реальный HTTP-статус `/robots.txt` и `/sitemap.xml` на проде на момент выполнения — неизвестен до фактического запуска.
- Реальная структура существующих карточек (`data-vin`, `data-stage` атрибуты) — код содержит эвристики извлечения VIN/стадии по регуляркам и обобщённым ключевым словам; если реальная вёрстка отличается, Gate C/baseline confidence может не набрать уверенность и корректно заблокирует релиз (fail-closed), а не наврёт про успех.
- Фактическое существование `/home/Carix/robots.txt` на корневом уровне — неизвестно; если его нет, Gate A заблокирует релиз с точным текстом причины.

## Рекомендация для ChatGPT/Codex ревью
1. Проверить синтаксис `python3 -m py_compile cloud/seo_korea_kyiv_001/seo_korea_kyiv_001.py` в CI.
2. Прогнать `FORCE_PA_MODE=1` на изолированной тестовой копии `/home/Carix`-подобной структуры (temp dir) для сухого прогона без реального прода, если такая тестовая fixture нужна отдельным раундом.
3. Только после этого владелец утверждает реальный запуск через GitHub-раннер с настоящим `PYTHONANYWHERE_API_TOKEN`.
