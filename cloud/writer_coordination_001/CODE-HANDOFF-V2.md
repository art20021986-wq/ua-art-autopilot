# Complete17 v2: точный установщик и серверная проверка

Новый `code_handoff_v2.py` сохраняет проверенный алгоритм v1, но принимает только actual final17 v2 manifest `12afce821b784771a2a8a0415cc289f5e713d53aabedf0ed3e2747a8c4d4e136` / `UA-ART-SPEC-AUTO10-COMPLETE-17-V2`. Версия схемы квитанции — 2. Старый код, manifest и его доказательства сохранены.

SHA нового установщика: `08e013756a1ffe89043904efeb878d1d7318d67cdb5c628573f3b26cb454fc55`.

Изоляционный комплект `package_handoff_v2_gate.py` воспроизводимо собирает пять файлов. Проверка конкурирующего писателя использует отдельный fork-процесс, наследующий Python audit guard; произвольные subprocess и сеть запрещены. Все файлы и шесть lock-inode тестовые, создаются в новом каталоге запуска.

ZIP `code-handoff-v2-gate.zip`: `a9332e5af9a5d5518b51b281840aabeff3152d1d4d005f378a542a18c0bb4f6f`, 19 941 байт. Manifest комплекта: `a88430b0339f25fd403d349e0cc9df9ff060297e0f97c3add7c4fa1d1b337d56`.

Каталог распаковки должен быть новым: `/home/Carix/code_handoff_v2_gate_20260909/bundle-v2`. После независимой проверки SHA архива штатная команда стенда:

```sh
python3.10 -B /home/Carix/code_handoff_v2_gate_20260909/bundle-v2/handoff_v2_server_gate.py
```

Локальная проверка того же комплекта с включённым guard: **23/23 PASS**, ошибок/пропусков 0, заблокированных операций 0, исходные файлы неизменны. Протокол — `evidence/handoff-v2-guard-local-result.json`. Это локальная проверка; результат PythonAnywhere будет отдельным фактическим протоколом.

`prepare_maintenance_v2.py` подготовил конкретный список 17 устанавливаемых путей и их v2 SHA, четырёх сохраняемых задач, webapp `www.uaart.com.ua`, исходников проверяющего маршрута, backup/rollback и необходимых доказательств: `evidence/maintenance-v2-readiness.json`.

Данные до установки не выдуманы: все отсутствующие актуальные SHA обозначены `UNOBSERVED`, что не равно отсутствию файла. Состояние webapp.enabled и текущая execution identity также требуют свежего наблюдения. Скрипт не создаёт разрешение владельца и не объявляет подготовленный документ исполняемым планом.

Точный оставшийся блокер: документированный API PythonAnywhere позволяет управлять задачами и webapp, но не предоставляет полного доказательства завершения старых процессов WSGI/cron/manual. Интерфейс показывает процессы и действие Kill; это само по себе не является подтверждённым завершением всех прежних писателей. До такого подтверждения код не получает реальный `verify_window`, не устанавливается в production и не разрешает возобновление задач.
