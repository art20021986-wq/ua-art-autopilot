name: task087-conflicting-owner-target-fail-closed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  conflicting-target:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse conflicting Korea target before external access
        run: |
          echo 'TASK 087 conflicts with the visible owner ferry directive.'
          echo 'No PythonAnywhere request was made. Explicit owner clarification is required.'
          exit 1
