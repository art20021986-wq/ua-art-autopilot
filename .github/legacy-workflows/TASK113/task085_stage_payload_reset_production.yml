name: task085-superseded-fail-closed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse superseded Korea target before external access
        run: |
          echo 'TASK 085 is superseded by TASK 086. No PythonAnywhere request was made.'
          exit 1
