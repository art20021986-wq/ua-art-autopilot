name: TASK092 UA WEB RECOVERY AUTOSTART

on:
  # Disabled after the owner restored GitHub Actions and Anthropic limits and
  # the dedicated TASK092 LAUNCH AFTER LIMIT UPDATE worker started. Keep this
  # manual-only to prevent duplicate Claude calls and unnecessary API spend.
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Automatic duplicate launcher disabled; use the active TASK092 worker.'
