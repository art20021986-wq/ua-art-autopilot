name: task086-superseded-by-task087-fail-closed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse superseded ferry target before external access
        run: |
          echo 'TASK 086 ferry target is superseded by TASK 087 Korea target.'
          echo 'No PythonAnywhere request was made.'
          exit 1
