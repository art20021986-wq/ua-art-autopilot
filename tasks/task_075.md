# TASK 075 — CRM-CATALOG-STAGE-GUARD-003 v1.0

OWNER_APPROVAL: `УТВЕРЖДАЮ CRM-CATALOG-STAGE-GUARD-003 v1.0. В РАБОТУ.`

## Разрешённый режим

`BACKUP → SANDBOX/CANARY`. Production запрещён до отдельной письменной команды владельца.

## Цель

1. Провести GET-only аудит CRM, двух каталогов и UA-0011.
2. Подтвердить и устранить в sandbox две причины дефекта: незавершённую media-синхронизацию и текстовый fallback без фотографии.
3. Нормализовать четыре публичных этапа из единственного CRM `status`.
4. Обеспечить ровно одну карточку в одной категории этапа, с фотографией и единым шаблоном.
5. Проверить UA-0001…UA-0011, отдельно UA-0009 и переходы 1→2→3→4.

## Публичная логика

- Корея → `korea`.
- Паром → `more`, текст «На пароме · маршрут — Киев».
- Грузия → `gruzia`, текст «В Грузии · маршрут — Киев».
- Киев → `kiev`, текст «В Киеве · можно посмотреть».
- `транзит` и `предоплата внесена` публично не показывать.

## Ворота

- `crm.db` только `mode=ro` + `query_only`.
- PythonAnywhere API только GET.
- До transform фиксируется SHA/размер всех входов.
- UA-0011: CRM-фото > 0, main photo разрешена, card count=1, stage/category совпадают.
- 11/11 уникальных карточек; UA-0009 PASS.
- Любая ошибка завершает sandbox FAIL. Production не затрагивается.

ROUND 2: canary обязан заменить старый маршрут этапа 2 во всех существующих
паромных карточках, а не только в UA-0011.

ROUND 3: визуальная приёмка обязательна. Все 11 карточек в обоих canary должны
быть заново собраны одним шаблоном, использовать разрешённое абсолютное фото и
не зависеть от прежней разметки `article`/`plitka`/text fallback. `<base>` для
локального canary ставится до CSS-ссылок. Этап 3 считает 15 дней от фактической
даты перехода, а не от внутреннего признака оплаты.

ROUND 4: визуальная проверка обязана подтвердить не только наличие URL фото,
но и HTTP 200/206 с `image/*`. Все карточки получают совместимый штатному сайту
`data-stage`, группируются 4→3→2→1 в одном управляемом блоке, а UA-0011
переносится из внешнего fallback внутрь общей последовательности. Каталожные
фото загружаются eager, чтобы mobile canary не скрывал сбой lazy-loading.
