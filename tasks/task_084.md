# TASK 084 — UA-0011-KOREA-CARD-RESET-001 v1.0

## Последняя прямая команда владельца

29.08.2026 в 07:38:18 UTC владелец уточнил актуальное состояние Hyundai SONATA 2018
(`UA-0011`, VIN `KMHE341DBKA544289`):

1. Перевести карточку в этап **«В Корее»** (канонический CRM status `kr_bought`).
2. Удалить номер контейнера.
3. Удалить дату прибытия и не вычислять срок доставки, пока нет подтверждённой погрузки.
4. Вернуть полноценную карточку единого шаблона со всей имеющейся информацией и фотографиями.
5. Первая фотография обязательна как фасадная/обложка.
6. Отсутствие отдельных данных не должно менять структуру карточки.
7. Исправить первопричину в общем механизме этапов и генераторе, чтобы будущие карточки не получали урезанный fallback-шаблон и не попадали на неверный этап.
8. При техническом сбое повторять только безопасный запуск; не запускать одновременно два writer-процесса.

Это задание **заменяет TASK 082**. Любая логика TASK 082, возвращающая UA-0011 «На пароме», считается устаревшей и должна завершаться fail-closed.

## Разрешённый режим этого запуска

Только fresh read-only live audit + backup metadata + изолированный sandbox/canary.
Запись в production/CRM/PythonAnywhere этим заданием не разрешена до отдельной письменной команды владельца на production.
Runtime LLM tokens: 0.

## Gate A — выполнить автоматически

- Получить свежую строку UA-0011 и точные имена полей статуса, контейнера и даты без записи.
- Зафиксировать SHA/значения всех остальных полей и полный media manifest.
- Подтвердить собственную первую фасадную фотографию UA-0011 и отсутствие чужих media.
- Найти все callback/API/фоновые writer-пути, способные менять status/container/date.
- Построить sandbox-копию с единственной целевой дельтой:
  - `status -> kr_bought`;
  - поле контейнера -> `NULL`/пусто согласно канонической схеме;
  - поле даты прибытия -> `NULL`/пусто согласно канонической схеме;
  - прочие CRM-поля и media неизменны.
- Пересобрать preview полной карточки тем же renderer, что и остальные карточки: фото + основной блок, без узкого текстового fallback.
- Добавить постоянные guards:
  - смена этапа в CRM является единственным источником публичного этапа;
  - контейнер/ETA не могут сохраняться при `kr_bought`;
  - отсутствие media mapping блокирует запись каталога до завершения media sync;
  - повтор команды идемпотентен.
- Проверить UA-0001…UA-0011 и отдельно безопасность UA-0009.
- Выполнить минимум 10 повторных sandbox-прогонов: дубли 0, дрейф 0, неожиданные изменения 0.
- Обновить `cloud/latest_status.md`, `cloud/owner_reply.md` и подробный отчёт под `cloud/task_084_ua0011_korea_reset/`.

## Обязательный итог Gate A

Выдать PASS/BLOCKED с точной матрицей before/after, тестами, preview/evidence и явным
`PRODUCTION_TOUCHED: NO`. При любом несовпадении — fail-closed без частичной записи.
