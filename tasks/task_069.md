# TASK 069 — CRM-CONTAINER-KYIV-DAYS-001 v1.0

STATUS: IN_WORK
MODE: READ-ONLY AUDIT + ISOLATED CANDIDATE
PRODUCTION_WRITE: NO
CRM_DB_WRITE: NO
LLM_TOKENS_IN_RUNTIME: 0

## Исправить

1. Снять актуальные `cars_ui.py`, `konteyner.py`, `db.py` и схему `crm.db`; сначала найти точную причину, почему после кнопки «Ввести номер контейнера» сообщение принимается, но `cars.sea_container` не сохраняется.
2. Исправить обработчик: номер 4–20 латинских букв/цифр нормализовать в верхний регистр, записать только в явно открытую карточку, проверить read-back и лишь после успеха очистить состояние ожидания. Ответ: `✅ Контейнер сохранён: <номер>`. Ошибка не должна молча сбрасывать ожидание.
3. В едином меню `📦 Контейнер, даты и сроки` добавить ровно одну кнопку `⏱ Количество дней до Киева` → существующий маршрут `car_setf:<id>:eta_days`. Если такая кнопка уже реально есть — не дублировать.
4. Срок хранить действующей логикой `eta_manual` + `days_to_kyiv`; допустимо 0–400 дней. Ручное значение важнее автоматических 15 дней и ежедневно уменьшается по календарю.
5. Правило действует для всех существующих и будущих карточек. Остальные кнопки, этапы, цены, фото, видео, диагностику и публикацию не менять.

## Обязательные тесты

- UA-0011: `ONEYSELGF1046602` сохраняется, виден после повторного открытия меню и после перезапуска тестового процесса.
- Неверный номер отклоняется; другая карточка не изменяется; повторная отправка идемпотентна.
- Кнопка дней видна ровно один раз; ввод числа сохраняет обе колонки и показывает дату Киева.
- Регрессия UA-0009 и всех обнаруженных карточек; `sqlite quick_check=ok`; непредусмотренных изменений 0.

## Выход

Только изолированный кандидат, тесты, hashes до/после и короткий отчёт. Production/CRM не изменять и сервисы не перезапускать без отдельной письменной команды владельца.
