# TASK 074 — CATALOG-CARD-UNIFY-002 v1.0

OWNER_APPROVAL: `УТВЕРЖДАЮ CATALOG-CARD-UNIFY-002 v1.0. В РАБОТУ`

## Цель

Во всех 11 действующих и всех будущих карточках каталога удалить из нижнего
VIN/ETA-блока дублирующую фразу «Автомобиль на пароме: Корея → Грузия.» и её
украинский эквивалент. Верхний зелёный `status-pill` «На пароме / Маршрут:
Корея → Грузия» сохранить как единственный индикатор этапа.

Нижний VIN/ETA-блок объединить с основным блоком карточки: одинаковый фон,
без разрыва и отдельной рамки, одна тонкая внутренняя разделительная линия.

## Ограничения

- Менять только `stranica.py`, `yadro.py`, `master_card.py` и два `katalog.html`.
- CRM, строки БД, media, индивидуальные страницы и ссылки не менять.
- Применить через общий генератор, без списка идентификаторов.
- Проверить 11 карточек, отдельно UA-0009, RU/UA и повторную генерацию.
- Обязательны exact-SHA base gate, shadow, persistent backup, atomic replace,
  bot restart, immediate + delayed public check и rollback при любой ошибке.
- LLM tokens в runtime: 0.

## Приёмка

- Дублирующей фразы нет в `/video/katalog.html` и `/site/katalog.html`.
- Верхний зелёный этап и маршрут сохранены.
- ETA, VIN, двигатель и видео сохранены.
- Нижний и основной блоки имеют единый фон и соединены.
- UA-0009 присутствует и не повреждена.
- Все 11 canonical VIN-блоков уникальны.
- БД и индивидуальные страницы побайтно не изменились.
